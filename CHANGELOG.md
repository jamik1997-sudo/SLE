# SLE v6.5.7

- Исправлен HTTP 500 на `GET /audits?limit=30/50/100`.
  Причина: в `list_audits()` ошибочно использовалась несуществующая переменная `level`.
- `list_audits()` дополнительно защищён от отсутствующих старых связей employee/region/auditor.
- Исправлена зависимость фильтра Дашборда «Сотрудник → Оценивающий».
  При выбранном сотруднике из руководителей отображается только его закреплённый руководитель.
  Аудиторы и менеджеры остаются доступными согласно текущим правилам.
- Выбранный сотрудник больше не сбрасывается при обновлении списка оценивающих без причины.
- Оставлен только актуальный `CHANGELOG.md`.
