# SLE v6.2.4 — Dashboard Click + Backend Stability

- Реализована отсутствовавшая функция `completedVisitView()`.
- Клик по строке «Последние завершённые аудиты» открывает выбранную точку одним read-only листом.
- После автоприменения фильтра обработчик клика повторно привязывается к новым строкам.
- Просмотр группирует вопросы по шагам/разделам и показывает 1 / 0 / N/A и комментарии.
- Закреплены backend-исправления: `and_`, `current_user`, отсутствие `get_current_user`.
- Просмотр использует реальные поля `visit_number`, `question_key`, `answer_value`.
